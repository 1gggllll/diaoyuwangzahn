# 将本文件内容复制到 C:\xampp\apache\conf\httpd.conf 最末尾，然后重启 Apache
# 作用：网址 /phishguard 映射到 F:\three，无需把文件放到 C:\xampp\htdocs

Alias /phishguard "F:/three"
<Directory "F:/three">
    Options -Indexes FollowSymLinks
    AllowOverride All
    Require all granted
</Directory>
